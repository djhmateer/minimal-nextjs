export default function LoadingState() {
    return (
        <div className="p-4">
            <h1 className="text-2xl mb-4">Loading users...</h1>
            <p>Please wait while we fetch the user data.</p>
        </div>
    );
}